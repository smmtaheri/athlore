# Helm (placeholder)

Helm charts for Athlore are intentionally not implemented in this milestone.

This directory is reserved for a future Kubernetes packaging effort.
Use Docker Compose (`../compose.yaml`) for current deployments.
