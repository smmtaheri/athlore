# API and authentication responses must not be cached by Nginx or an
# intermediary CDN. Hide any upstream value so this response policy is final.
proxy_hide_header Cache-Control;
add_header Cache-Control "no-store" always;
proxy_no_cache 1;
proxy_cache_bypass 1;
