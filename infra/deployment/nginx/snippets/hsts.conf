# HTTPS is terminated by the ParsPack CDN for all three Athlore hostnames.
# includeSubDomains is intentionally omitted because only the three canonical
# names are in scope for this deployment.
add_header Strict-Transport-Security "max-age=31536000" always;
